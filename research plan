# WVWAM 三方案研究计划

## 目标

代码只保留三个 diffusion 方案：

1. `original`
2. `goal_reverse_independent`
3. `goal_reverse_joint`

统一配置入口：

```yaml
training:
  world_generation_variant: original
```

## 方案一：Original WVWAM

### 条件与目标

输入为任务指令、当前多视角图像、当前机器人状态，以及加噪后的 forward video/action。
目标保持正向时间顺序：

```text
video:  x_t -> ... -> x_{t+H}
action: a_t -> ... -> a_{t+H-1}
```

Video Expert 与 Action Expert 在同一次 forward 中扩散去噪。Action Expert 可以读取
video KV，保持原始 WVWAM 的 video/action 关联。

### Loss

```text
L = L_video_diffusion + L_action_diffusion
```

配置：

```text
validation_tmp/original_wvwam.yml
```

## 方案二：Independent Goal-Conditioned Reverse Diffusion

### Key 预测

Key-video 基于：

```text
任务指令 + 当前帧 latent + video noise + video timestep
```

从纯噪声多步采样未来终点画面 `x_hat_{t+H}`。

Key-action chunk 基于：

```text
任务指令 + 当前多视角图像 + 当前机器人状态
+ action noise + action-dimension mask + action timestep
```

预测：

```text
a_hat_{t+H-1}, ..., a_hat_{t+H-K}
```

Key-video 与 key-action 使用两次独立 forward，相互不可见。

### Reverse 生成

训练目标先翻转为：

```text
video_reverse  = x_{t+H}, ..., x_t
action_reverse = a_{t+H-1}, ..., a_t
```

Predicted keys 被 detach 并固定：

- Video Expert 以 predicted key-video 为 anchor 独立生成 reverse video；
- Action Expert 以 predicted key-action chunk 为 prefix 独立生成 reverse action。

最后将 reverse action 翻回正序执行。

### Loss

```text
L_video =
    L_key_video
  + L_reverse_video

L_action =
    L_key_action
  + L_reverse_action
```

四项当前权重均为 `1.0`。

配置：

```text
validation_tmp/goal_reverse_independent.yml
```

## 方案三：Joint Goal-Conditioned Reverse Diffusion

### Key 联合预测

Key-video noise 和 key-action noise 在同一次 model forward 中去噪：

```text
pred_key_video, pred_key_action = model(
    noisy_key_video,
    noisy_key_action,
    instruction,
    current_observation,
)
```

Video Expert 保持自身视频建模；Action Expert 读取 video/action KV，因此采用有向分解：

```text
p(video, action | condition)
= p(video | condition) * p(action | video, condition)
```

### Reverse 联合生成

固定并 detach 两类 predicted keys 后，在每个 diffusion timestep 中同时更新：

```text
noisy reverse video
+ noisy reverse action
-> joint model
-> video/action scheduler step
-> clamp key-video 与 key-action prefix
```

固定 key 位置不参与 reverse loss，它们分别由 key loss 监督。

### Causal Attention

```yaml
architecture:
  model:
    video_frame_causal_self_attn: true
    action_video_frame_causal_kv: true
    action_expert:
      frame_causal_self_attn: true
```

Reverse index `i` 只能读取已经位于其前面的 reverse video/action token。

### Loss

Loss 形式与 independent 版本一致，但 key 和 reverse 分别采用联合 forward：

```text
L =
    L_key_video
  + L_key_action
  + L_reverse_video
  + L_reverse_action
```

配置：

```text
validation_tmp/goal_reverse_joint.yml
```

## 三方案配置矩阵

| 设置 | Original | Independent Goal Reverse | Joint Goal Reverse |
|---|---:|---:|---:|
| `reverse_world_order` | false | true | true |
| key-video/action | 无 | 独立预测 | 联合预测 |
| reverse video/action | 联合正向扩散 | 独立逆向扩散 | 联合逆向扩散 |
| video causal self-attn | false | true | true |
| action causal self-attn | false | true | true |
| action-to-video causal KV | false | false | true |

## 训练命令

```bash
CUDA_VISIBLE_DEVICES=0,1 accelerate launch --num_processes 2 main_world.py \
  --model_config_path=validation_tmp/original_wvwam.yml

CUDA_VISIBLE_DEVICES=0,1 accelerate launch --num_processes 2 main_world.py \
  --model_config_path=validation_tmp/goal_reverse_independent.yml

CUDA_VISIBLE_DEVICES=0,1 accelerate launch --num_processes 2 main_world.py \
  --model_config_path=validation_tmp/goal_reverse_joint.yml
```

## 验证指标

三方案共同比较：

- action MSE/L1
- video latent L1
- video L1
- PSNR
- SSIM
- RoboTwin online success rate

Goal 方案额外记录：

- `loss_gc_key_video`
- `loss_gc_key_action`
- `loss_gc_reverse_video`
- `loss_gc_reverse_action`
- key-video latent L1
- key-action MSE/L1

Joint 关联性验证采用 batch shuffle：

1. 正常匹配 video/action；
2. batch 内打乱 video；
3. 比较 action MSE 和 success rate。

如果打乱 video 后 action 几乎不变，说明 Action Expert 尚未有效利用 video 条件。

## Checkpoint 兼容

WAM checkpoint 加载器只提取：

```text
video_expert.*
action_expert.*
```

历史 checkpoint 中的 Stage 2/3/4 辅助参数会被忽略。三个正式方案应分别完成：

- 配置解析测试；
- 训练 loss smoke test；
- 多步推理 shape/clamp/flip 测试；
- checkpoint state-dict 过滤测试；
- RoboTwin adapter smoke test。











####################################################### 当前 Causal Attention 方案详细说明

### 1. 时间方向与 Causal 的定义

Original WVWAM 使用正常时间顺序：

```text
x_t, a_t -> ... -> x_{t+H}, a_{t+H}
```

两个 Goal-Conditioned Reverse 方案在进入模型前同时翻转 video 和 action：

```text
reverse index 0, 1, ..., H
=
physical time t+H, t+H-1, ..., t
```

因此，本文中的 causal attention 是“reverse 序列方向上的因果性”。对于 reverse
位置 `i`，它只能读取 reverse 位置 `j <= i` 的 token，不能读取 `j > i`。

这与正向物理时间上的 causal 恰好方向相反，但与部署流程一致：

1. 先根据任务指令和当前状态预测 horizon key；
2. 从 horizon key 开始；
3. 按 reverse 顺序生成通向当前状态的视频和动作；
4. 最后把 reverse action 翻回正常时间顺序执行。

需要特别说明：当前生成器仍然是 diffusion，不是逐 token 的自回归模型。训练时模型会
同时接收整段带噪序列，推理时也会同时迭代更新整段 noisy trajectory。Causal mask 的
作用是限制每个位置的信息来源，防止其利用 reverse 方向上尚不应该可见的位置；它不会
把 diffusion 自动变成 AR decoder。

### 2. Video Frame Causal Self-Attention

配置接口：

```yaml
architecture:
  model:
    video_frame_causal_self_attn: true
```

视频 latent 经过 patch embedding 后形成：

```text
[frame 0 的 P 个空间 token,
 frame 1 的 P 个空间 token,
 ...
 frame F-1 的 P 个空间 token]
```

设 query token 属于视频帧 `f_q`，key token 属于视频帧 `f_k`，可见条件为：

```text
M_video(q, k) = allowed, if f_k <= f_q
                masked,  if f_k > f_q
```

同一帧内的全部空间 token 可以互相注意，因此该方案是“帧级 causal”，不是把图像内部
的 patch 也按一维顺序做 causal。

在 reverse 序列中：

- key-video 位于 frame 0；
- reverse frame 1 可以读取 key-video 和自身；
- reverse frame `i` 可以读取 frame `0...i`；
- 它不能读取 reverse frame `i+1...F-1`。

实现位置：

- `wam/models/mvwam/frame_causal_mask.py`
  - `build_video_frame_causal_bool_mask`
  - `build_video_frame_causal_additive_mask`
- `wam/models/mvwam/video_expert.py`
  - Video Expert 单独 forward 时应用该 mask
- `wam/models/mvwam/world_policy_causal_value.py`
  - video/action 联合 forward 时同样应用该 mask

实现这一 mask 的原因：

1. 防止某个 reverse 视频帧读取 reverse 方向上尚未生成的后续帧，避免训练信息泄漏；
2. 让训练时的信息可见范围与“从 key-video 向当前帧逆向生成”的部署过程一致；
3. 保留每帧内部完整的空间建模能力，不破坏预训练 Video Expert 的图像结构先验；
4. 使 key-video 真正成为整条 reverse video trajectory 的起始锚点，而不是仅作为附加特征。

### 3. Action Frame Causal Self-Attention

配置接口：

```yaml
architecture:
  model:
    action_expert:
      frame_causal_self_attn: true
```

Action Expert 的 self-attention 序列由三部分组成：

```text
[robot-state prefix, action tokens, optional value tokens]
```

当前可见规则为：

1. Robot-state prefix query 只能读取 prefix；
2. 第 `i` 个 action query 可以读取全部 prefix 和 action `0...i`；
3. action `i` 不能读取 action `i+1...H`；
4. optional value query 可以读取完整序列。

形式化表示：

```text
action_query(i) -> prefix + action[0:i]
```

当前三个正式配置均设置 `use_value: false`，因此 value token 的全局可见规则目前不会影响
实际训练。

在 reverse 方案中，action 序列最前面的 `key_action_chunk_size=4` 个位置是 horizon
key-action chunk。后续 reverse action 只能读取该 key prefix 和已经位于其前面的
reverse action。

实现位置：

- `wam/models/mvwam/frame_causal_mask.py`
  - `build_action_self_causal_bool_mask`
  - `build_action_self_causal_additive_mask`
- `wam/models/mvwam/action_expert.py`
  - Action Expert 单独 forward 时应用该 mask
- `wam/models/mvwam/world_policy_causal_value.py`
  - 联合 forward 时把该 mask 合并到 action attention mask

实现这一 mask 的原因：

1. 防止 action `i` 直接读取未来的 reverse action GT/noisy token；
2. 使 action 的依赖方向与 key-action -> reverse action 的生成顺序一致；
3. 强制模型学习动作之间的局部因果连续性，而不是依赖完整 chunk 的双向信息；
4. 为后续滚动规划或更严格的 action autoregressive 解码保留兼容的信息结构。

### 4. Action-to-Video Frame Causal KV

配置接口：

```yaml
architecture:
  model:
    action_video_frame_causal_kv: true
```

该开关只在 Joint Goal-Conditioned Reverse Diffusion 中开启。联合 attention 的方向不是
video/action 完全对称：

```text
Video query  -> Video K/V
Action query -> Video K/V + Action K/V
```

因此联合模型实现的是有向分解：

```text
p(video, action | condition)
= p(video | condition) * p(action | video, condition)
```

Video Expert 保持自身视频动力学建模，不读取 action token；Action Expert 可以读取与
当前 reverse 时间相匹配的 video token。

当 action 长度 `A` 与视频帧数 `F` 不相同时，第 `a` 个 action step 对应的最大可见视频
帧通过 end-of-bin 对齐计算：

```text
video_cutoff(a) = ((a + 1) * F - 1) // A
```

其可见规则为：

```text
action_query(a) -> video_frame[0 : video_cutoff(a)]
```

此外：

- robot-state prefix 只读取 video frame 0；
- optional value query 可以读取全部 video frame；
- 每个可见视频帧内部的全部空间 token 都可被 action query 使用。

例如 `A=4, F=2` 时，四个 action step 的 video cutoff 为：

```text
[0, 0, 1, 1]
```

这样既支持 action/video 等长，也支持二者不同采样率下的单调时间对齐。

实现位置：

- `wam/models/mvwam/frame_causal_mask.py`
  - `map_action_steps_to_video_frames`
  - `build_action_to_video_frame_causal_bool_mask`
- `wam/models/mvwam/world_policy_causal_value.py`
  - `build_action_joint_attention_mask`
  - `forward_joint`

实现这一 mask 的原因：

1. 恢复 video generation 与 action generation 的关联，使动作能够参考模型生成的视频状态；
2. 防止早期 reverse action 偷看更晚才应出现的视频帧；
3. 在 action/video 时间分辨率不一致时建立明确、单调的对齐关系；
4. 保持依赖方向为 video -> action，避免 video 和 action 相互读取造成循环条件；
5. 保留预训练 Video Expert 的动力学主干，同时让 Action Expert 学习“看到该状态后应采取何种动作”。

### 5. 三方案的 Causal Profile

| 方案 | Video self causal | Action self causal | Action-to-Video causal KV |
|---|---:|---:|---:|
| Original WVWAM | false | false | false |
| Independent Goal Reverse | true | true | false |
| Joint Goal Reverse | true | true | true |

Independent 方案虽然对 video 和 action 各自使用 causal self-attention，但 key 阶段和
reverse 阶段分别调用 Video Expert 与 Action Expert，因此没有联合的 action-to-video
causal KV。它主要验证“逆序 + predicted keys + 单模态 causal”本身是否有效。

Joint 方案在 Independent 基础上增加 action-to-video causal KV，并在 key 阶段和
reverse 阶段都联合预测 video/action，用于验证显式跨模态时序关联是否能改善 action
误差和 RoboTwin success rate。

`FMPRunner` 初始化时会严格检查 profile：

```text
original                 -> (false, false, false)
goal_reverse_independent -> (true,  true,  false)
goal_reverse_joint       -> (true,  true,  true)
```

这里的 tuple 顺序是：

```text
(video self causal, action self causal, action-to-video causal KV)
```

如果配置与方案不匹配，Runner 会直接抛出错误，避免实验名称与实际 attention 行为不一致。

### 6. Mask 的工程实现

三个 mask 最终都转换为 additive attention bias：

```text
允许位置 -> 0
禁止位置 -> torch.finfo(dtype).min
```

新的 causal bias 会与已有 attention mask 相加，而不是覆盖已有的数据 mask 或
video-KV mask。只要启用了显式 additive mask，attention backend 会切换到支持该 mask
的 native backend。

配置从 YAML 到模型的路径为：

```text
validation_tmp/*.yml
-> configs/model_config_loader.py
-> wam/models/mvwam/build_model.py
-> Video Expert / Action Expert / WorldPolicyModel
```

需要区分两个容易混淆的接口：

```yaml
causal_world_training: true
```

该字段只用于选择并校验 `train_world` 联合训练入口，不代表 causal attention 已开启。
真正控制 attention 的是：

```yaml
video_frame_causal_self_attn
action_expert.frame_causal_self_attn
action_video_frame_causal_kv
```

### 7. 与 Predicted Key 的关系

Causal attention 与 predicted key condition 共同定义 reverse 生成：

```text
任务指令 + 当前状态
-> diffusion 预测 key-video/key-action
-> detach predicted keys
-> keys 写入 reverse 序列起点
-> causal diffusion 生成其余 reverse video/action
-> reverse action 翻回正序执行
```

其中：

- key-video 通过 `latent_inpainting` 写入视频首帧条件通道；
- key-action chunk 通过 `prefix` 写入 action 序列前缀；
- key 位置在 reverse loss 中被屏蔽，由独立的 key loss 监督；
- causal mask 保证其余位置只能从 key 及 reverse 前缀获取时序信息。

如果只有 predicted key 而没有 causal mask，后续位置仍可能利用整段 reverse noisy
trajectory 的双向信息；如果只有 causal mask 而没有 predicted key，模型又缺少明确的
目标状态锚点。两者需要配合使用。

### 8. 当前限制与后续验证

1. 当前 causal diffusion 不是严格的逐帧 AR 解码器，整段轨迹仍在每个 diffusion
   timestep 中同步更新；
2. 当前 horizon 仍由 `fixed_horizon` 定义，尚未替换为 CoA semantic keypoint；
3. Joint 目前只实现 video -> action 的有向条件，没有 action -> video 的反向注意力；
4. Native attention backend 支持任意 mask，但速度可能低于无 mask 的优化 backend；
5. Causal mask 能防止结构性信息泄漏，但不能保证模型一定有效利用 video 条件。

因此需要以下验证：

1. 检查三方案 causal profile 是否与配置矩阵一致；
2. 检查 mask 单元测试，包括同帧可见、未来帧不可见和 action/video 不等长映射；
3. 对 Joint 方案执行 batch 内 video shuffle，观察 action MSE 是否显著恶化；
4. 比较 Independent 与 Joint 的 key loss、reverse loss 和 RoboTwin success rate；
5. 比较 causal on/off 的显存、吞吐和采样时延；
6. 可视化 attention 或对 video condition 做遮挡，确认 Action Expert 是否真实使用视频状态。
